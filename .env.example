# =============================================================================
# Environment configuration for flyway-split
#
# Copy this file to .env and fill in the values before running docker-compose.
#
#   cp .env.example .env
#
# For non-local environments, use scripts/fetch-secrets.sh to populate .env
# from your secrets backend instead of editing it by hand:
#
#   SECRETS_BACKEND=ssm ENV=staging ./scripts/fetch-secrets.sh
#
# .env is git-ignored and must never be committed.
# =============================================================================


# -----------------------------------------------------------------------------
# Database connection
#
# Owner: Platform team
#
# Local docker-compose:  DB_HOST=db  (the docker-compose service name)
# Deployed environments: DB_HOST is the real RDS/Cloud SQL hostname
#
# SSM paths:
#   /development/flyway-split/db-host
#   /development/flyway-split/db-port
#   /development/flyway-split/db-name
#
# Secrets Manager / Vault: keys db-host, db-port, db-name within
#   /development/flyway-split/database
# -----------------------------------------------------------------------------
DB_HOST=CHANGE_ME
DB_PORT=CHANGE_ME
DB_NAME=CHANGE_ME


# -----------------------------------------------------------------------------
# Superuser password  (Platform team only — DBAs and CI/CD pipelines)
#
# SSM:              /development/flyway-split/postgres-password  (SecureString)
# Secrets Manager:  key postgres-password in /development/flyway-split/database
# Vault:            field postgres-password in secret/development/flyway-split/database
# -----------------------------------------------------------------------------
POSTGRES_PASSWORD=CHANGE_ME


# -----------------------------------------------------------------------------
# Application user password  (handoff credential: Platform team sets, bookstore
# squad reads — this is the only secret the squad needs)
#
# SSM:              /development/flyway-split/app-user-password  (SecureString)
# Secrets Manager:  key app-user-password in /development/flyway-split/database
# Vault:            field app-user-password in secret/development/flyway-split/database
# -----------------------------------------------------------------------------
APP_USER_PASSWORD=CHANGE_ME


# -----------------------------------------------------------------------------
# Reporting user password  (Platform team only)
#
# SSM:              /development/flyway-split/reporting-user-password  (SecureString)
# Secrets Manager:  key reporting-user-password in /development/flyway-split/database
# Vault:            field reporting-user-password in secret/development/flyway-split/database
# -----------------------------------------------------------------------------
REPORTING_USER_PASSWORD=CHANGE_ME
